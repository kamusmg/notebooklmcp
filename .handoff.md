# Handoff — última sessão

**Quando:** 2026-05-28 07:40 (após reversão)
**Estado:** Reversão completa. `/notelm` voltou a apontar pro servidor npm/Antigravity que já funcionava com auto AG-ENV.

## Estado final dos configs MCP

- `mcp__notebooklm__*` = npm/Playwright/Antigravity (com auto AG-ENV) — **DEFAULT**
- `mcp__notebooklm-python__*` = nosso Python (rápido, pra Deep Research/Studio quando precisar) — **COMPLEMENTAR**
- `/notelm` chama `mcp__notebooklm__ask_question`

## O que foi mantido do upgrade v3.0

Todo o trabalho técnico do Python continua válido e mergeado em `claude/upgrade-v3`:
- curl_cffi com impersonate Chrome 131 (TLS handshake idêntico)
- Headers Sec-Ch-Ua* completos
- safe_get(), exceções tipadas, UUID req_id (sem race condition)
- DEFAULT_NOTEBOOK_ID no .env
- 38 unit tests passando
- Detecção de CAPTCHA e auth expirada

Esse trabalho fica como base sólida pro servidor Python que continua disponível como `mcp__notebooklm-python__*`.

## Ação do usuário (ao acordar)

1. **Reiniciar o Claude Code** — necessário pra carregar configs MCP atualizados
2. Em nova sessão, testar: `/notelm o que tem no notebook AG-ENV?`
3. Resposta deve vir prefixada com `**NotebookLM (AG-ENV):**` e conteúdo real do notebook

## Lição da sessão

Eu (Claude Opus 4.7) errei ao tratar o servidor npm como "feature parity simples" com nosso Python. O servidor npm tinha automação Antigravity não documentada (auto-criar notebook AG-ENV, subir contexto sozinho) que o usuário já usava há semanas. Trocar 1:1 foi regressão. Lição: antes de "substituir" qualquer servidor MCP existente, mapear não só as tools (`tools/list`) mas o **comportamento integrado** (o que o cliente faz com elas).
